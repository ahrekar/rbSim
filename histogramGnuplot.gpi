binwidth=1
set yrange [0:*]
#set xrange [0:.2]
set boxwidth binwidth
set style fill solid 1.0
bin(x,width)=width*floor(x/width)
plot 'electrons.dat' using (bin($3,binwidth)):(1.0) smooth freq with boxes
